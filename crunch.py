import os

# Make sure that the crunch.py script is inside your package directory before you run it.. example: (myapp/crunch.py), and remember to make sure your package includes the __main__.py file.. example: (myapp/__main__.py)

os.system("clear && python -m compileall __main__.py")

os.system("clear")
